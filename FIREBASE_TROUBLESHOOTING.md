# 🔧 Решение проблем с Firebase

## Проблема: "export declarations may only appear at top level of a module"

Эта ошибка возникает, когда Firebase SDK пытается использовать ES модули, но загружается как обычный скрипт.

## Решения:

### 1. Очистить кэш браузера

Браузер может кэшировать старую версию Firebase. Попробуйте:
- **Chrome/Edge:** Ctrl+Shift+Delete → Очистить кэш
- **Firefox:** Ctrl+Shift+Delete → Кэш
- Или откройте в режиме инкогнито (Ctrl+Shift+N)

### 2. Проверить версию Firebase в браузере

1. Откройте консоль браузера (F12)
2. Перейдите на вкладку Network
3. Обновите страницу (F5)
4. Найдите файлы `firebase-app.js` и `firebase-database.js`
5. Проверьте, какая версия загружается (должна быть 8.6.8)

### 3. Если версия 8.6.8 не работает

Попробуйте использовать еще более старую версию. Замените в `index.html`:

```html
<!-- Старая стабильная версия -->
<script src="https://www.gstatic.com/firebasejs/8.3.3/firebase-app.js"></script>
<script src="https://www.gstatic.com/firebasejs/8.3.3/firebase-database.js"></script>
```

### 4. Альтернатива: использовать полный bundle

Если отдельные скрипты не работают, используйте полный bundle:

```html
<script src="https://www.gstatic.com/firebasejs/8.6.8/firebase.js"></script>
```

Затем в `firebase-config.js` используйте:
```javascript
database = firebase.database();
```

### 5. Проверить блокировщики рекламы

Некоторые блокировщики рекламы могут блокировать скрипты Firebase. Попробуйте:
- Отключить блокировщик рекламы
- Добавить сайт в белый список

### 6. Проверить консоль на другие ошибки

Откройте консоль (F12) и проверьте, есть ли другие ошибки, которые могут мешать загрузке Firebase.

---

## Текущая конфигурация

В файле `index.html` используется:
- Firebase SDK версии **8.6.8**
- Скрипты загружаются в конце `<body>`
- Порядок: Firebase → firebase-config.js → script.js

---

## Если ничего не помогает

Можно временно использовать только localStorage (без Firebase):
1. В `firebase-config.js` закомментируйте инициализацию Firebase
2. Приложение будет работать, но без синхронизации между пользователями
3. Данные будут сохраняться только локально в браузере

