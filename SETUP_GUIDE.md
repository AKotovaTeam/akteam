# 🚀 Пошаговая инструкция по настройке трекера

## Шаг 1: Настройка Firebase (10 минут)

### 1.1. Создание проекта Firebase

1. Откройте https://console.firebase.google.com/
2. Войдите в свой Google аккаунт
3. Нажмите **"Создать проект"** (или "Add project")
4. Введите название проекта (например: `refactoring-tracker`)
5. Нажмите **"Продолжить"**
6. Отключите Google Analytics (не обязательно) или оставьте включенным
7. Нажмите **"Создать проект"**
8. Дождитесь создания проекта (30-60 секунд)
9. Нажмите **"Продолжить"**

### 1.2. Создание Web-приложения

1. В Firebase Console найдите иконку **</>** (Web) или кнопку **"Add app"** → **"Web"**
2. Введите название приложения (например: `tracker-app`)
3. **НЕ** ставьте галочку на "Also set up Firebase Hosting" (нам это не нужно)
4. Нажмите **"Зарегистрировать приложение"** (Register app)
5. **ВАЖНО:** Скопируйте конфигурацию Firebase, которая появится на экране
   Она выглядит так:
   ```javascript
   const firebaseConfig = {
     apiKey: "AIza...",
     authDomain: "your-project.firebaseapp.com",
     projectId: "your-project-id",
     storageBucket: "your-project.appspot.com",
     messagingSenderId: "123456789",
     appId: "1:123456789:web:abcdef"
   };
   ```
6. Нажмите **"Далее"** и затем **"Продолжить в консоли"**

### 1.3. Создание Realtime Database

1. В левом меню Firebase Console найдите **"Realtime Database"**
2. Нажмите **"Создать базу данных"** (Create database)
3. Выберите регион (например: `europe-west1` для Европы или `us-central1` для США)
4. Нажмите **"Далее"**
5. Выберите **"Начать в тестовом режиме"** (Start in test mode)
6. Нажмите **"Готово"**
7. **ВАЖНО:** Скопируйте URL базы данных (он будет вверху страницы)
   Выглядит так: `https://your-project-id-default-rtdb.firebaseio.com/`

### 1.4. Настройка правил безопасности

1. В Realtime Database найдите вкладку **"Правила"** (Rules)
2. Замените содержимое на:
   ```json
   {
     "rules": {
       "refactoringTracker": {
         ".read": true,
         ".write": true
       }
     }
   }
   ```
3. Нажмите **"Опубликовать"** (Publish)

⚠️ **Внимание:** Эти правила позволяют всем читать и писать. Для команды это нормально, но не делитесь ссылкой публично!

### 1.5. Обновление конфигурации в проекте

1. Откройте файл `firebase-config.js` в вашем проекте
2. Замените все значения на скопированные из Firebase Console:

```javascript
const firebaseConfig = {
    apiKey: "ВАШ_API_KEY_ИЗ_FIREBASE",
    authDomain: "ВАШ_PROJECT_ID.firebaseapp.com",
    projectId: "ВАШ_PROJECT_ID",
    storageBucket: "ВАШ_PROJECT_ID.appspot.com",
    messagingSenderId: "ВАШ_MESSAGING_SENDER_ID",
    appId: "ВАШ_APP_ID",
    databaseURL: "https://ВАШ_PROJECT_ID-default-rtdb.firebaseio.com/"
};
```

**Пример:**
```javascript
const firebaseConfig = {
    apiKey: "AIzaSyC1234567890abcdefghijklmnopqrstuv",
    authDomain: "refactoring-tracker.firebaseapp.com",
    projectId: "refactoring-tracker",
    storageBucket: "refactoring-tracker.appspot.com",
    messagingSenderId: "123456789012",
    appId: "1:123456789012:web:abcdef123456",
    databaseURL: "https://refactoring-tracker-default-rtdb.firebaseio.com/"
};
```

---

## Шаг 2: Развертывание на GitHub Pages (5 минут)

### 2.1. Создание репозитория на GitHub

1. Откройте https://github.com/
2. Войдите в свой аккаунт
3. Нажмите **"+"** в правом верхнем углу → **"New repository"**
4. Введите название (например: `refactoring-tracker`)
5. Выберите **"Public"** (или Private, если хотите)
6. **НЕ** ставьте галочки на "Add a README file" и другие
7. Нажмите **"Create repository"**

### 2.2. Загрузка файлов через терминал

Откройте терминал в папке проекта и выполните:

```bash
# Инициализация git (если еще не сделано)
git init

# Добавление всех файлов
git add .

# Создание первого коммита
git commit -m "Initial commit: Refactoring tracker with Firebase"

# Добавление удаленного репозитория (замените YOUR_USERNAME и YOUR_REPO)
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git

# Отправка на GitHub
git branch -M main
git push -u origin main
```

**Пример:**
```bash
git remote add origin https://github.com/alyona/refactoring-tracker.git
```

### 2.3. Включение GitHub Pages

1. На GitHub откройте ваш репозиторий
2. Перейдите в **Settings** (Настройки)
3. В левом меню найдите **"Pages"**
4. В разделе **"Source"** выберите:
   - Branch: `main`
   - Folder: `/ (root)`
5. Нажмите **"Save"**
6. Подождите 1-2 минуты
7. Ваш сайт будет доступен по адресу:
   `https://YOUR_USERNAME.github.io/YOUR_REPO/`

**Пример:**
`https://alyona.github.io/refactoring-tracker/`

---

## Шаг 3: Проверка работы

1. Откройте ваш сайт в браузере
2. Откройте тот же сайт в другом браузере или на другом устройстве
3. В первом окне отметьте несколько MRs как завершенные
4. Во втором окне изменения должны появиться автоматически через несколько секунд

---

## Шаг 4: Поделитесь ссылкой

Отправьте ссылку на сайт (например: `https://alyona.github.io/refactoring-tracker/`) программистам:
- Жене
- Юре
- Роме

Все они смогут:
- Видеть прогресс в реальном времени
- Отмечать завершенные MRs
- Назначать задачи друг другу

---

## ❓ Частые проблемы

### Проблема: "Firebase не работает"
**Решение:**
1. Проверьте, что все значения в `firebase-config.js` заполнены правильно
2. Убедитесь, что Realtime Database создана
3. Проверьте правила безопасности в Firebase Console
4. Откройте консоль браузера (F12) и посмотрите на ошибки

### Проблема: "GitHub Pages не работает"
**Решение:**
1. Убедитесь, что все файлы загружены в репозиторий
2. Проверьте, что в Settings → Pages выбран правильный branch
3. Подождите 2-3 минуты после сохранения настроек

### Проблема: "Изменения не синхронизируются"
**Решение:**
1. Проверьте, что Firebase конфигурация правильная
2. Убедитесь, что правила безопасности в Firebase позволяют чтение и запись
3. Проверьте консоль браузера (F12) на ошибки

---

## 📞 Нужна помощь?

Если что-то не работает:
1. Откройте консоль браузера (F12 → Console)
2. Посмотрите на ошибки (красные сообщения)
3. Скопируйте ошибки и проверьте инструкцию выше

---

## ✅ Чеклист готовности

- [ ] Firebase проект создан
- [ ] Web-приложение добавлено в Firebase
- [ ] Realtime Database создана
- [ ] Правила безопасности настроены
- [ ] `firebase-config.js` заполнен правильными значениями
- [ ] Репозиторий создан на GitHub
- [ ] Файлы загружены в GitHub
- [ ] GitHub Pages включен
- [ ] Сайт открывается по ссылке
- [ ] Синхронизация работает (проверено в двух окнах)

Готово! 🎉

